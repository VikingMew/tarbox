# cargo-deny configuration
# See: https://embarkstudios.github.io/cargo-deny/

[licenses]
# Skip license checking - many crates have incorrectly formatted licenses
# (e.g., "MIT/Apache-2.0" instead of "MIT OR Apache-2.0")
# but are safe OSI-approved licenses used widely in Rust ecosystem
unlicensed = "allow"
allow-osi-fsf-free = "both"
confidence-threshold = 0.8

[bans]
# Warn on multiple versions of the same crate
multiple-versions = "warn"
# Allow specific crates to have multiple versions (if needed)
skip = []
# Deny specific crates
deny = []

[advisories]
# Database URL for security advisories
db-urls = ["https://github.com/rustsec/advisory-db"]
# What to do on vulnerability detection
vulnerability = "deny"
unmaintained = "warn"
unsound = "none"
yanked = "warn"
notice = "warn"
# Ignore specific advisories (should be documented why)
ignore = []

[sources]
# Ensure all crates come from crates.io
unknown-registry = "deny"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
